import React from 'react'
import { AuthTemplate } from 'UI'

export function Register() {
  return (
    <>
      <AuthTemplate
        title="Login"
        // isLoading={isLoading}
        // isError={error !== null}
        // errorMessage={error ?? ''}
      >
        <div>Register</div>
      </AuthTemplate>
    </>
  )
}
